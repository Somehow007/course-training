spring:
  application:
    name: tp
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/course_training?autoReconnect=true&useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: Sh20060401
  servlet:
    session:
      timeout: 30m
    context-path: /

server:
  port: 8090
  tomcat:
    uri-encoding: utf-8
    threads:
      max: 200

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml

logging:
  level:
    root: info
    com.ujs.trainingprogram.tp: debug
    org.springframework.web: warn
