<?xml version="1.0" encoding="UTF-8"?>
<!--
  ~ Licensed to the Apache Software Foundation (ASF) under one or more
  ~ contributor license agreements.  See the NOTICE file distributed with
  ~ this work for additional information regarding copyright ownership.
  ~ The ASF licenses this file to You under the Apache License, Version 2.0
  ~ (the "License"); you may not use this file except in compliance with
  ~ the License.  You may obtain a copy of the License at
  ~
  ~     http://www.apache.org/licenses/LICENSE-2.0
  ~
  ~ Unless required by applicable law or agreed to in writing, software
  ~ distributed under the License is distributed on an "AS IS" BASIS,
  ~ WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  ~ See the License for the specific language governing permissions and
  ~ limitations under the License.
  -->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ujs.trainingprogram.tp.dao.mapper.CourseMapper">

    <!-- 分页查询课程 -->
    <select id="pageCourseResults" parameterType="com.ujs.trainingprogram.tp.dto.req.course.CoursePageQueryReqDTO"
            resultType="com.ujs.trainingprogram.tp.dto.resp.course.CoursePageQueryRespDTO">
        SELECT
        c.id AS courseId,
        c.course_name AS courseName,
        c.course_nature AS courseNature,
        c.college_id AS collegeId,
        c.del_flag AS delFlag,
        dic.dict_name AS courseType,
        col.college_name AS collegeName
        FROM
        course c
        LEFT JOIN sys_dict dic ON c.dict_id = dic.id
        LEFT JOIN college col ON c.college_id = col.id
        WHERE 1=1
        <if test="requestParam.courseIds != null and requestParam.courseIds.size() >= 0">
            AND c.id IN
            <foreach collection="requestParam.courseIds" item="courseId" open="(" separator="," close=")">
                #{courseId}
            </foreach>
        </if>
        <if test="requestParam.courseNature != null">
            AND c.course_nature = #{requestParam.courseNature}
        </if>
        <if test="requestParam.courseNames != null and requestParam.courseNames.size() >= 0">
            AND (
            <foreach collection="requestParam.courseNames" item="courseName" open="" separator=" OR " close="">
                c.course_name LIKE CONCAT('%', #{courseName}, '%')
            </foreach>
            )
        </if>
        <!-- 动态处理字典条件：根据 ID 或名称查询 -->
        <if test="requestParam.dictIds != null and requestParam.dictIds.size() > 0">
            AND c.dict_id IN
            <foreach collection="requestParam.dictIds" item="dictId" open="(" separator="," close=")">
                #{dictId}
            </foreach>
        </if>
        <if test="requestParam.dictNames != null and requestParam.dictNames.size() > 0">
            AND dic.dict_name IN
            <foreach collection="requestParam.dictNames" item="dictName" open="(" separator="," close=")">
                #{dictName}
            </foreach>
        </if>
        <if test="requestParam.collegeIds != null and requestParam.collegeIds.size() > 0">
            AND c.college_id IN
            <foreach collection="requestParam.collegeIds" item="collegeId" open="(" separator="," close=")">
                #{collegeId}
            </foreach>
        </if>
        <if test="requestParam.collegeNames != null and requestParam.collegeNames.size() > 0">
            AND col.college_name IN
            <foreach collection="requestParam.collegeNames" item="collegeName" open="(" separator="," close=")">
                #{collegeName}
            </foreach>
        </if>
        ORDER BY c.create_time DESC
    </select>


</mapper>